



/* 
const randomNumber = Math.random(); 
let result = '';
let guess = ''; 

function pickComputerMove() {
  // computer move
  if (randomNumber < 0.5) {
    result = 'heads';
  } else {
    result = 'tails';
  }
}

function userPickedHeads() {
  if (result === 'heads') {
    guess = 'You win!'; 
  } else {
    guess = 'You lose!'; 
  }

  alert(`You pick heads. Result is ${result}. ${guess}`);
}

function userPickedTails() {
  if (result === 'tails') {
    guess = 'You win!'; 
  } else {
    guess = 'You lose!'; 
  }

  alert(`You pick tails. Result is ${result}. ${guess}`);
}
*/ 
